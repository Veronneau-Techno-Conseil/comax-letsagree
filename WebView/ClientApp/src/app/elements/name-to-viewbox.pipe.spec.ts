import { NameToViewboxPipe } from './name-to-viewbox.pipe';

describe('NameToViewboxPipe', () => {
  it('create an instance', () => {
    const pipe = new NameToViewboxPipe();
    expect(pipe).toBeTruthy();
  });
});
