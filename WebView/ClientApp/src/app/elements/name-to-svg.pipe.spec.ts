import { NameToSvgPipe } from './name-to-svg.pipe';

describe('NameToSvgPipe', () => {
  it('create an instance', () => {
    const pipe = new NameToSvgPipe();
    expect(pipe).toBeTruthy();
  });
});
